version: '2.2'

services:
    pwn-server:
        build: ./
        image: skyepodium/pwn-server
        # external:internal
        ports:
            - "60001:8080"
        expose:
            - "8080"
        pids_limit: 1024
        cpus: 0.5
        restart: unless-stopped
